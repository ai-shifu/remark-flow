{
  "name": "remark-flow",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc --build --clean && tsc --build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "unist": "^0.0.1",
    "unist-util-visit": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
